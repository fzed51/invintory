# Yarn 4 utilise Plug'n'Play par défaut ; ce projet dépend d'un node_modules
# classique (binaires résolus via node_modules/.bin, volume Docker monté sur
# /app/node_modules).
nodeLinker: node-modules
